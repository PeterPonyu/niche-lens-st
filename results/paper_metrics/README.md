# NicheLens-ST paper_metrics stubs

These JSON files are **contract stubs** — they establish the per-repo
metrics-stub schema (matching the `l-*`/`a-*` stubs in the mono-repo root)
but contain no real results yet.

| File | Asset ID | Role |
|------|----------|------|
| `n-f1_metrics_stub.json` | N-F1 | Niche map figure |
| `n-f2_metrics_stub.json` | N-F2 | Scalability / hyperparameter sweep figure |
| `n-t1_metrics_stub.json` | N-T1 | Multi-method comparison table |

All stubs have `paper_claim_ready: false` and `supports_safe_prose: false`.
They will be promoted once real benchmark runs are validated and the claim
ledger entries are finalised.
